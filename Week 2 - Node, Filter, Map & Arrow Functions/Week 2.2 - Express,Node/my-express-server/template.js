const express = require("express");
const app = express();
const port = 3000;

app.get(one_parameter,second_parameter);
// one_parameter = Path
// second_parameter = callbackfunction(request,respone)
app.post(one_parameter,second_paramter)
app.listen(port_number)
